# Checkpoints

Place optional checkpoints here when running the learned TF evidence front end:

- `sim_classifier.pth`
- `field_adapter.pth`

The included unit tests and regression checks do not require checkpoint files.
