# Data Directory

This directory is reserved for user-provided datasets, generated TF512 observations, or result tables.

The upload-ready repository does not include generated simulation data, raw RF recordings, paper result CSV/JSON files, or trained checkpoints.

Relevant scripts accept explicit input/output paths, so large artifacts can stay outside the Git repository.
